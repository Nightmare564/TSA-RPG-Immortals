/// Get User Input
XAxis = keyboard_check(vk_right) - keyboard_check(vk_left); // Movement
YAxis = keyboard_check(vk_down) - keyboard_check(vk_up);

Attack = keyboard_check(vk_enter);
Melee = keyboard_check(vk_space);